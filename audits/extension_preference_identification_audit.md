# Extension Preference / Governance-Interface Identification Audit — Results

## Provenance

Preregistered before execution at:

```text
efcf3a780c50b64780ee2b21c93b7af5a8f86b5d
```

Parent checkpoint:

```text
ca423e1029b013368c4281944af5a02678af83c5
```

Preregistration blob:

```text
4136ccf08bf4dab92baf68ae7a5f5a06f902355c
```

The GitHub connector independently verified the exact preregistration blob before publication. The local execution harness did not mount repository files, so the committed executable contains an exact Git-blob verification path for normal in-repository execution and a connector-verified fallback used only in this connector execution environment.

The actual 12-candidate native comparison graph was not read, deserialized, enumerated, queried, or used during calibration. Its upstream artifact identifiers are retained as lineage anchors only.

Correct provenance:

\[
\boxed{\textbf{
fresh preference/governance-interface identification result with inherited lineage anchors and preregistered hard controls.
}}
\]

No actual-candidate preference, `Q_extension`, governance-family ranking, governance justification, adoption, authorization, binding, or execution is introduced here.

---

## 1. Frozen endpoint

The audit executes only:

\[
\boxed{
(\mathcal R^{\rm cal},G_{\rm pref})
\longrightarrow
R_{\rm pref}.
}
\]

Observed calibration universe:

```text
synthetic relation profiles                17
supplied governance contracts               4
preregistered contract/profile cases       49
anonymous nuisance encodings               64
primary preference records               3136
actual candidate graph reads                 0
actual candidate pair evaluations            0
actual candidate preference relations        0
```

All `3136 / 3136` preregistered target relations and warrant codes were recovered.

The gate therefore tests preference identification relative to supplied governance, not governance selection.

---

## 2. Primary factorization result

For each supplied governance contract, the child architecture constructed the governance-interface state

\[
q_G(\mathcal R)
\]

before consulting the preregistered target labels.

Factorization was then tested as:

\[
\boxed{
\exists h_G:\quad L_G=h_G\circ G_{\rm pref}.
}
\]

Observed:

| supplied contract | fibers | max fiber | multi-label fibers | factorization |
|---|---:|---:|---:|---|
| `G_PARTIAL_EMPTY` | 9 | 5 | 0 | PASS |
| `G_CONSTRAINT_B` | 7 | 3 | 0 | PASS |
| `G_LEX_DV_REOPEN_B` | 7 | 2 | 0 | PASS |
| `G_COMP_EXPLICIT` | 9 | 2 | 0 | PASS |

Each result held under all `64/64` anonymous encodings.

Therefore every primary governance-interface fiber contains exactly one preregistered candidate-level target relation plus compatible warrant semantics.

This is the finite-regime positive result for **no governance aliasing**.

---

## 3. Lawful quotienting is present

The primary architecture does not pass factorization merely by copying the full raw relation profile.

Under `G_PARTIAL_EMPTY`, the following five profiles:

```text
P_ALIGN_I
P_GEOM_GAP_IGNORED
P_COLL_INC_IGNORED
P_REOPEN_LICENSE_GAP
P_REOPEN_INCOMPARABLE
```

share one governance-interface fiber and one target:

```text
PREFER_I / ALIGNED_OBJECTIVES
```

because `DeltaC`, collateral, and reopenability have no preference authority under that supplied contract.

Likewise:

```text
P_EQ
P_GEOM_INC_ALL_EQ
```

share one fiber with:

```text
EQUIVALENT / ALL_RELEVANT_EQUIVALENT
```

So the positive interface **forgets unauthorized distinctions while preserving every distinction required by the supplied target relation**.

---

## 4. Nuisance invariance — no authority injection

For every primary contract/profile case, the execution generated nuisance variants with identical authorized semantics.

Exact results:

```text
candidate-handle invariance       3136 / 3136
synthetic-provenance invariance   3136 / 3136
serialization invariance          3136 / 3136
contract-alias invariance         3136 / 3136
implementation-tag invariance     3136 / 3136
fixture-ID invariance             3136 / 3136
opaque-nonce invariance           3136 / 3136
```

The primary decision path reads only the native relation fields authorized by the supplied contract and the semantic contents of:

```text
Gamma_license
Gamma_constraint
Gamma_objective
Gamma_tradeoff
```

It does not read candidate handles, provenance labels, fixture IDs, display aliases, nonce values, insertion order, or implementation tags.

Thus:

\[
\boxed{
(\mathcal R,G)_a\equiv(\mathcal R,G)_b
\Rightarrow
R_{\rm pref}(a)=R_{\rm pref}(b)
}
\]

held exactly in the audited nuisance regime.

---

## 5. Deliberate governance-aliasing controls

Three deliberately lossy governance interfaces were tested only as falsifiers.

| control | multi-label fibers in exemplar | factorization failures |
|---|---:|---:|
| `A1_DROP_REQUIRED_DELTAV` | 3 | 64/64 |
| `A2_NLC_AS_EQ` | 2 | 64/64 |
| `A3_INC_AS_EQ` | 1 | 64/64 |

`A1` drops an authoritative `DeltaV` distinction.

`A2` collapses required `NO_LICENSED_COMPARISON` into equality.

`A3` collapses required `INCOMPARABLE` into equality.

Each creates at least one governance-interface fiber containing multiple preregistered target relations.

Therefore:

\[
\boxed{
\textbf{governance aliasing is empirically detectable in the preregistered finite controls.}
}
\]

The broken alias interfaces remain nuisance-invariant themselves, so their failure is collapse, not authority injection.

---

## 6. Deliberate authority-injection controls

Three nuisance-sensitive broken interpreters were tested independently:

```text
I1_OPAQUE_NONCE
I2_PROVENANCE
I3_HANDLE_LEXICAL
```

Observed:

```text
I1 authority injection detected    64 / 64
I2 authority injection detected    64 / 64
I3 authority injection detected    64 / 64
```

For all three controls, the primary governance state remained decision-sufficient and the primary output stayed invariant.

Thus these failures do not require governance aliasing.

Therefore:

\[
\boxed{
\textbf{authority injection is empirically detectable independently of governance aliasing in the preregistered finite controls.}
}
\]

This establishes the intended two-sided diagnostic separation:

\[
\boxed{
\begin{array}{c|c}
\text{governance aliasing} & \text{authorized distinction lost}\\
\text{authority injection} & \text{unauthorized distinction added}
\end{array}
}
\]

within this calibration domain.

---

## 7. Pair-swap symmetry

Every primary relation was recomputed after candidate-role reversal.

Observed:

```text
pair-swap relation + warrant law   3136 / 3136
```

The frozen laws held exactly:

```text
PREFER_I  <-> PREFER_J
EQUIVALENT -> EQUIVALENT
NO_WARRANTED_PREFERENCE -> NO_WARRANTED_PREFERENCE

CONSTRAINT_BLOCK_I <-> CONSTRAINT_BLOCK_J
all other warrant reasons retain their semantic identity
```

No candidate handle or lexical ordering participates in the primary relation.

---

## 8. Typed abstention is preserved

The audit preserves three distinct non-resolution loci:

| condition | candidate-level result | failure locus |
|---|---|---|
| required native `NO_LICENSED_COMPARISON` | `NO_WARRANTED_PREFERENCE / LICENSE_GAP` | `NATIVE_LICENSE` |
| required native `INCOMPARABLE` | `NO_WARRANTED_PREFERENCE / NATIVE_INCOMPARABILITY` | `NATIVE_PARTIALITY` |
| licensed objective conflict with empty tradeoff authority | `NO_WARRANTED_PREFERENCE / NO_TRADEOFF_AUTHORITY` | `GOVERNANCE` |

Therefore:

\[
\boxed{
\texttt{NO\_LICENSED\_COMPARISON}
\neq
\texttt{INCOMPARABLE}
\neq
\texttt{NO\_WARRANTED\_PREFERENCE}.
}
\]

The child execution diagnoses these loci only. It does not acquire evidence, rewrite native relations, modify `G_pref`, or execute any repair.

The frozen boundary remains:

\[
\boxed{\textbf{repair authority follows failure locus.}}
\]

No repair authority is exercised in this gate.

---

## 9. Empty tradeoff authority works as an abstention-capable contract

`G_PARTIAL_EMPTY` has:

\[
\Gamma_{\rm tradeoff}=\varnothing.
\]

The conflicting profiles:

```text
P_CONFLICT_I_BURDEN
P_CONFLICT_J_BURDEN
```

therefore yield:

```text
NO_WARRANTED_PREFERENCE / NO_TRADEOFF_AUTHORITY
```

in every anonymous encoding.

No compensation rate, numeric weight, hidden priority, or tie-break is invented.

This verifies that preference identification does not require forced completion.

---

## 10. Supplied constraint, priority, and tradeoff provenance remains visible

The four primary contracts are calibration environments:

```text
G_PARTIAL_EMPTY
G_CONSTRAINT_B
G_LEX_DV_REOPEN_B
G_COMP_EXPLICIT
```

Every result record retains:

```text
governance_provenance = SUPPLIED_CALIBRATION_GOVERNANCE
Gamma_* canonical representations and SHA-256 checksums
structured B_G ledger
calibration profile ID
anonymous encoding ID
```

The finite compensatory clauses `T1/T2` are recorded as supplied governance.

An `AUTHORIZED_TRADEOFF` result means only:

> the mechanism applied the explicitly supplied calibration clause.

It does **not** mean that the clause was empirically or normatively justified.

No governance family is ranked.

No `B_G` minimality analysis is performed.

---

## 11. Same relation profile, different supplied governance

The frozen cross-governance discriminant uses:

```text
P_CONFLICT_I_BURDEN
```

Observed exactly:

```text
G_PARTIAL_EMPTY
  -> NO_WARRANTED_PREFERENCE / NO_TRADEOFF_AUTHORITY

G_CONSTRAINT_B
  -> PREFER_J / CONSTRAINT_BLOCK_I

G_LEX_DV_REOPEN_B
  -> PREFER_I / LEXICOGRAPHIC_PRIORITY

G_COMP_EXPLICIT
  -> PREFER_I / AUTHORIZED_TRADEOFF
```

Thus:

\[
\boxed{
\mathcal R
\text{ alone does not determine }
R_{\rm pref}.
}
\]

Different outputs here are not authority injection because the supplied authoritative governance input changed.

---

## 12. Wrong-control / anti-cheat status

All preregistered shortcut classes remain absent from the primary path:

```text
NLC -> neutral/equality                false
INC -> tie/equality                    false
missing relation -> numeric penalty   false
Scope -> compensable utility          false
one favorable objective -> global     false when authorized conflict exists
post-hoc weights                      false
actual graph -> G_pref tuning          false
provenance -> preference              false
tradeoff success -> justification      false
hidden nuisance tie-break             false
```

The primary architecture contains no numeric scalarization, candidate score, utility, reward, or common scale.

---

## 13. Holdout integrity

The actual candidate graph remained a strict holdout:

```text
actual_candidate_graph_read                 false
actual_candidate_pair_evaluations               0
actual_candidate_preference_relations           0
actual_candidate_ids_in_calibration_payload     0
```

The prior graph's commit/blob identifiers occur only as provenance anchors.

No graph content or graph-derived pattern enters calibration.

Therefore the result cannot be attributed to post-hoc tuning on the 12 actual candidates.

---

## 14. Anti-downstream status

```text
preference identification performed              true
actual-candidate preference application          false
overall actual-candidate preference               false
Pareto filtering                                  false
candidate ranking                                 false
candidate score                                   false
utility                                           false
reward                                            false
Q_extension                                       false
NO_WARRANTED_ADOPTION                             false
adoption                                          false
authorization                                     false
binding                                           false
execution                                         false
governance repair                                 false
governance reopenability                          false
mathfrak_G_min                                    false
```

This gate identifies only the supplied-governance preference interface in the synthetic calibration regimes.

---

## 15. Earned claim

The strongest permitted claim is:

\[
\boxed{
\textbf{
In the audited finite preference-calibration regimes, candidate-level preference relations are identifiable relative to explicitly supplied governance contracts: the primary governance interfaces preserve the decision distinctions required by their preregistered target relations, remain invariant to non-authoritative nuisance variables, preserve typed no-preference boundaries, and retain the provenance of supplied constraint, priority, and tradeoff structure.
}
}
\]

The control result additionally supports:

> Deliberately lossy governance interfaces produce preregistered governance-aliasing failures, and deliberately nuisance-sensitive interpreters produce preregistered authority-injection failures in these audited finite calibration regimes.

This does **not** establish:

- that any supplied governance contract is normatively correct;
- generality outside the frozen calibration families;
- a universal recursive-governance law;
- governance reopenability;
- governance repair;
- `mathfrak_G_min`;
- actual-candidate preference;
- scalar sufficiency;
- `Q_extension`;
- adoption;
- authorization;
- binding.

The actual 12-candidate graph remains available for a later separately preregistered preference-application gate. It is not touched here.
