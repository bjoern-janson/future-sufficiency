# Decision-Substrate Language Identification Architecture — Non-Semantic Erratum

Parent architecture freeze:

```text
fe21bbe2cd48bd71011674edd16488b5a492f291
```

This erratum corrects exactly one typographical error in Section 28 of:

```text
audits/extension_decision_substrate_language_identification_architecture_preregistration.md
```

The required serialization field printed there as:

```text
aonymous_candidate_count
```

is bindingly corrected to:

```text
anonymous_candidate_count
```

No scientific object, definition, admissibility rule, burden rule, closure rule, outcome state, calibration constraint, provenance boundary, success criterion, stop condition, or downstream flag changes.

The architecture semantics remain exactly those frozen at `fe21bbe2cd48bd71011674edd16488b5a492f291`, with this field-name correction applied for all later machine serialization.