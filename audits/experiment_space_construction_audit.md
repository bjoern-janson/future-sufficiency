# Experiment-Space Construction Discriminant — Results

## Provenance

Preregistered before execution at:

```text
fd99c09e6d65dd0e73a6909bd38fc1d8d55c6bb3
```

Gate-1 anchor:

```text
c661e58  latent-registry minimality audit
```

The Gate-2 panel was executed freshly in-session against the preregistered logic. The committed executable imports the Gate-1 audit and recursively hard-asserts the accessibility / valuation / navigation chain when run in-repo. Those older audits were **not** freshly process-replayed in this connector session.

Correct provenance:

\[
\boxed{\textbf{fresh Gate-2 experiment-space-construction result with inherited hard regression assertions}.}
\]

No autonomous-discovery or authorization artifact is introduced by this result.

---

## 1. Exhaustive old-closure novelty proof

The old Gate-1 substrate `S0` was recomputed from the four coordinate primitives plus XOR closure.

Observed:

```text
|C0| = 15
C0 checksum = 809abd918f17a77c779f76ce1b14ba4661e63b6834c4bec475fbe8a8f8eeed49
```

The checksum exactly matches Gate 1.

Across 64 anonymous encodings, each frozen nonlinear target `n1...n4` was absent from the recomputed old closure:

```text
n1 outside C0: 64/64
n2 outside C0: 64/64
n3 outside C0: 64/64
n4 outside C0: 64/64
```

Therefore the novelty criterion is satisfied extensionally:

\[
\boxed{\Pi(n_k)\notin\mathcal C_0\quad\forall k.}
\]

No novelty claim depends on a new name, syntax tree, output polarity, or incomplete enumeration.

---

## 2. Decision relevance under the frozen contract

The old closure is not merely missing the exact target by name. Every old parity partition has zero correction relevance for every nonlinear context.

| target | baseline Bayes accuracy | exact target `R_corr` | exact margin `R_corr-kappa` | max old-closure `R_corr` |
|---|---:|---:|---:|---:|
| n1 | 0.750 | 0.250 | 0.150 | 0.000 |
| n2 | 0.750 | 0.250 | 0.150 | 0.000 |
| n3 | 0.750 | 0.250 | 0.150 | 0.000 |
| n4 | 0.625 | 0.375 | 0.275 | 0.000 |

with the frozen:

\[
\boxed{\kappa=0.1.}
\]

Thus `G20` correctly returns `COMMIT` on all 256 context-encoding evaluations.

This localizes the old-space failure upstream of valuation:

\[
\boxed{
\text{useful target exists}
\land
\Pi(e_{\rm target})\notin\mathcal C_0
\land
\text{the old construction substrate cannot expose it}.
}
\]

---

## 3. Primary expansion `G20 -> G21`

`G21` adds one generic target-blind `AND` operator and the preregistered post-extension XOR closure.

Observed:

```text
|C0| = 15
|C1| = 1023
C1 checksum = c51cc447000204a4c32e205d4e8daab954a52b80939664bbab2968629299efb7
C0 strict subset C1 = true

novel target reachability = 256/256
exact novel-target choice = 256/256
```

Every target partition is in:

\[
\boxed{\mathcal C_1\setminus\mathcal C_0.}
\]

The construction ledger changes:

```text
S0 B_construction = 8
S1 B_construction = 10
```

with zero target-specific semantic bindings or target hints in `S1`.

Therefore the preregistered causal signature holds:

\[
\boxed{
B_{\rm construction}\uparrow
\land
\mathcal C_0\subsetneq\mathcal C_1
\land
\text{new correction-relevant partitions become reachable}.
}
\]

Earned claim:

\[
\boxed{
\textbf{In this finite audited regime, changing the admitted experiment-composition substrate opens a strictly larger refinement space containing useful evidence partitions that were unreachable under the complete prior closure.}
}
\]

This is a **local causal basin-opening result relative to `S0`**.

It does **not** establish that the system discovered or authorized the `AND` extension.

---

## 4. `G22` — new syntax without new geometry

Adding `NOT` to `S0` changes the operator vocabulary but, because evidence is evaluated modulo output polarity:

```text
|C_NOT| = 15
C_NOT = C0
novel target reachability = 0/256
exact novel-target choice = 0/256
COMMIT = 256/256
```

Thus:

\[
\boxed{
\text{new operator syntax}
\neq
\text{experiment-space expansion}.
}
\]

---

## 5. `G23` — nonlinear operator without adequate post-extension closure

With direct `AND` over old `C0` experiments but no XOR recombination of newly nonlinear results:

```text
|C_G23| = 120
checksum = 8d3a5ecddbcf823c2ffca59f2490d2950caea216816b3c8e9af58bb2dfbb5dc1

n1 reachable = yes
n2 reachable = yes
n3 reachable = yes
n4 reachable = no

target reachability = 192/256
exact target choice = 192/256
```

The preregistered `3/4` ceiling is therefore exact and is not exceeded.

A useful nuance: the missing `n4` exact partition does **not** force COMMIT. `G23` contains 18 tied partial refinements with:

```text
best n4 partial R_corr = 0.125
best n4 partial margin = 0.025
```

so the frozen controller acquires a partial refinement, but the exact target partition remains unreachable and cannot be selected.

This preserves the intended construction conclusion:

\[
\boxed{
\text{nonlinear operator availability}
\neq
\text{adequate expanded compositional closure}.
}
\]

---

## 6. Anti-scaffold controls

### `G24` — direct target registry

Appending the four exact nonlinear target semantics yields 256/256 behavior, but it supplies four target-specific semantic bindings.

Classification:

\[
\boxed{\text{target-specific specification displacement}.}
\]

It is invalid as evidence for generic experiment-space construction.

### `G25` — opaque family macro

A transparent `ALL_DEGREE2_4` alias reproduces `G21`, but its expanded burden is the same `S1` burden:

```text
visible syntax burden = 1
expanded burden = 10
```

It provides no independent contraction or construction evidence.

A direct opaque macro implementation that stores the emitted family has:

```text
visible syntax burden = 1
expanded direct semantic bindings = 1023
```

and is classified as hidden specification.

Thus:

\[
\boxed{
\text{compact family macro}
\neq
\text{warranted experiment-space construction}.
}
\]

---

## 7. Anonymous-encoding invariance

Across all 64 encodings the audit permutes:

- primitive public handles;
- world labels;
- correction-context identifiers;
- emitted experiment handles / registry order;
- emitted binary output-token polarity.

Construction acts on internal semantic values; output-token polarity is randomized only at the emitted experiment interface, so public token relabeling cannot alter `AND`/`XOR` semantics.

All novelty, family-size, reachability, and choice results remain invariant.

---

## 8. Construction specification ledger

| condition | primitives | operators | grammar | semantic labels | generation constraints | target hints | expanded total | interpretation |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| G20 `S0` | 4 | 1 | 1 | 0 | 2 | 0 | 8 | old closure |
| G21 `S1` | 4 | 2 | 2 | 0 | 2 | 0 | **10** | valid target-blind expansion |
| G22 NOT | 4 | 2 | 2 | 0 | 2 | 0 | 10 | syntax-only; no new partitions |
| G23 one nonlinear layer | 4 | 2 | 2 | 0 | 2 | 0 | 10 | partial expansion; 3/4 target coverage |
| G24 direct targets | 4 | 1 | 1 | 4 | 2 | 0 | 12 | target-specific displacement |
| G25 transparent alias | — | — | 1 visible | — | — | — | 10 expanded | alias for S1 |
| G25 direct opaque macro | — | — | 1 visible | 1023 hidden bindings | — | — | 1023 expanded | hidden specification |

Gate 2 is deliberately **not** a compression result. The result is that a small target-blind increase in construction structure causally changes the reachable experiment geometry.

---

## 9. Gate boundary

The empirical Gate-2 result establishes:

\[
\boxed{
S_0\rightarrow S_1
\quad\Rightarrow\quad
\mathcal C_0\subsetneq\mathcal C_1
}
\]

and useful partitions in `C1 \ C0` become reachable and choice-maximal.

It does **not** establish:

- autonomous discovery of `AND`;
- diagnosis that `S0` should be changed;
- warranted selection among candidate substrate transformations;
- authorization to bind the extension;
- unrestricted experiment invention;
- general ontology construction;
- self-modification;
- that `AND` is uniquely necessary;
- that `C1` is minimal.

The next-order problem remains:

\[
\boxed{
\text{failure evidence}
\rightarrow
\text{diagnosis of closure insufficiency}
\rightarrow
\text{candidate substrate transformation}
\rightarrow
\text{authorization}
\rightarrow
\text{expanded refinement space}.
}
\]

That chain is not tested here.
