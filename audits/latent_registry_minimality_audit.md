# Latent-Registry Minimality Audit — Results

## Provenance

Preregistered at `8936606715ab39eaeec27426624f5d46ead7c2a2`.

Accessibility anchor: `6355333`.

The new Gate-1 registry panel was executed in-session against the preregistered logic. The committed executable imports the accessibility audit and hard-asserts the inherited valuation/navigation chain when run in-repo.

Correct provenance:

\[
\boxed{\textbf{fresh latent-registry-minimality result with inherited hard regression assertions}.}
\]

No Gate-2 artifact or theory file is changed.

---

## 1. Frozen question

Gate 1 only:

\[
\boxed{\mathcal E^\star_{\rm explicit}\rightarrow\mathcal E^\star_{\rm derived}}
\]

with the useful family held extensionally fixed at the same 15 nonzero four-bit parity partitions used by the accessibility audit.

Accessibility-source semantics checksum:

```text
54b7375f6ff283a3dff9f06bdaf67cced806411c9277021b0f9579c50cf45e59
```

Canonical partition-set checksum:

```text
809abd918f17a77c779f76ce1b14ba4661e63b6834c4bec475fbe8a8f8eeed49
```

No experiment outside this family is admitted as a success.

---

## 2. Primary result: R0 -> R1

R0 supplies 15 explicit experiment-semantic bindings:

\[
\boxed{B_{\mathcal E^\star}^{R0}=15.}
\]

R1 supplies:

```text
4 coordinate primitives
1 reusable XOR operator
1 target-blind recursive closure rule
2 generic generation constraints
```

with no semantic labels or target hints:

\[
\boxed{B_{\mathcal E^\star}^{R1}=8.}
\]

Across 64 anonymous encodings × 15 correction contexts:

```text
R0:
  recovered partitions       15/15 per encoding
  exact extensional equality 64/64
  extra partitions           0
  downstream choices         960/960

R1:
  recovered partitions       15/15 per encoding
  exact extensional equality 64/64
  extra partitions           0
  downstream choices         960/960
```

Therefore:

\[
\boxed{B_{\mathcal E^\star}:15\rightarrow8}
\]

while:

\[
\boxed{\Pi(\mathcal E^\star_{R1})=\Pi(\mathcal E^\star_{R0}).}
\]

Earned claim:

\[
\boxed{\textbf{Explicit enumeration of the 15 latent experiment semantics is not necessary for this fixed finite family; a smaller target-blind compositional substrate recovers the identical registry.}}
\]

This is registry compression only.

---

## 3. Coverage controls

### R2 — primitives only

Observed:

\[
\boxed{4/15}
\]

registry recall and:

\[
\boxed{256/960=4/15}
\]

downstream agreement, with precision 1 and zero extra partitions.

### R3 — one XOR layer

Observed:

\[
\boxed{10/15}
\]

registry recall and:

\[
\boxed{640/960=10/15}
\]

downstream agreement, again with precision 1 and zero extra partitions.

Neither control exceeds its extensional coverage ceiling.

Thus:

\[
\boxed{\text{operator availability}\neq\text{adequate compositional closure}.}
\]

---

## 4. Anti-scaffold controls

### R4 — opaque family macro

`ALL_PARITIES_4` can reproduce all 15 partitions and 960/960 decisions, but:

```text
visible burden  = 1
expanded burden = 15 target semantic obligations
```

Therefore:

\[
\boxed{\text{registry removed from syntax}\neq\text{registry removed from substrate}.}
\]

R4 is hidden specification / notation compression, not minimality evidence.

### R5 — context-target generator

A context-conditioned target generator can reproduce 960/960 decisions but requires 15 target-specific hints and does not recover a context-independent registry.

It is:

\[
\boxed{\text{oracle displacement}.}
\]

---

## 5. Specification ledger

| Representation | Visible burden | Expanded burden | Registry recovery | Downstream | Gate-1 evidence |
|---|---:|---:|---:|---:|---|
| R0 explicit | 15 | 15 | 15/15 | 960/960 | reference |
| R1 basis + XOR closure | 8 | 8 | 15/15 | 960/960 | **valid contraction** |
| R2 primitives only | 4 | 4 | 4/15 | 256/960 | insufficient |
| R3 one XOR layer | 7 | 7 | 10/15 | 640/960 | insufficient |
| R4 opaque macro | 1 | 15 | 15/15 | 960/960 | hidden specification |
| R5 context-target generator | 16 | 16 | no context-independent registry | 960/960 | oracle displacement |

---

## 6. What survived

The audit removes explicit registry enumeration as a necessary representation for this family.

It does **not** remove the need for a substrate sufficient to generate the required extensional family. The surviving functional structure is:

\[
\boxed{\text{primitive semantics}+\text{composition}+\text{adequate closure}.}
\]

The audit does not establish that the specific four-coordinate basis, XOR operator, or 8-clause representation is globally minimal.

---

## 7. Gate separation

Gate 1 is satisfied in the audited regime:

\[
\boxed{G_1:\ B_{\mathcal E^\star}\downarrow\land\mathcal E^\star\text{ unchanged extensionally}.}
\]

Gate 2 remains untouched:

\[
\boxed{G_2:\ \mathcal E_t^\star\subsetneq\mathcal E_{t+1}^\star.}
\]

No useful experiment outside the old recoverable family became available here. Therefore this result does not establish experiment invention, experiment-space expansion, ontology construction, \(\mathfrak R_{E,t}\to\mathfrak R_{E,t+1}\), or basin opening.

---

## 8. Upstream boundary

The child executable imports the accessibility audit and hard-asserts:

```text
A0 reachability / choice 960 / 960
A1 reachability / choice 960 / 960
A1 B_access              1
A2 reachability / choice 0 / 960
A3 reachability / choice 448 / 960
A4 minimality-valid      false
```

The older valuation/navigation audits are inherited hard assertions rather than a fresh process replay in this connector session.

The next repository action authorized by the sequence is only to update `P_EP_DEPENDENCY_CONSOLIDATION.md` with this Gate-1 result. Gate 2 is not started.
